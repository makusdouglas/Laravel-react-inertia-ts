import React from 'react';

// import { Container } from './styles';

const Home = () => {
    let foo = "React";
    const bar = "TypeScript";

    return (
        <h1>
            Hello {foo} + {bar}
        </h1>
    );
};

export default Home;
